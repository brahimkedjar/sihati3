<!DOCTYPE html>
<html>
<head>
  <title>Créer un compte</title>
  <style>
  .btn-primary {
            background-color: #70a5d7;
border-color:#70a5d7;
      border-radius: 0;
    }
    .card {
      max-width: 600px;
      margin: 50px auto;
      box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
    }

    .card-header {
background-color: #70a5d7;
border-color:#70a5d7;      color: #fff;
      padding: 20px;
    }

    .card-body {
      padding: 20px;
    }

    .form-group label {
      font-weight: bold;
    }

    .form-group .form-control {
      border-radius: 0;
    }

    .btn-primary {
      border-radius: 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="card-header">
        <h4 class="card-title mb-0">Créer un compte</h4>
      </div>
      <div class="card-body">
        <%= form_for(resource, as: resource_name, url: patient_registration_path, html: { class: 'needs-validation', novalidate: true }) do |f| %>
          <% if resource.errors.any? %>
            <div class="alert alert-danger">
              <h6 class="mb-0"><%= pluralize(resource.errors.count, "error") %> prevented this patient from being saved:</h6>
              <ul class="mb-0">
                <% resource.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <div class="form-group mb-3">
            <%= f.label :name, "Nom et prénom", class: "form-label" %>
            <%= f.text_field :name, class: "form-control", placeholder: "Entrez votre nom et prénom", required: true %>
          </div>
          <div class="form-group mb-3">
            <%= f.label :email, class: "form-label" %>
            <%= f.email_field :email, class: "form-control", placeholder: "Entrez votre Email", required: true %>
          </div>
          <div class="form-group mb-3">
            <%= f.label :phone_number, "Numéro de téléphone", class: "form-label" %>
            <%= f.text_field :phone_number, class: "form-control", placeholder: "Entrez votre numéro de téléphone", required: true %>
          </div>
          <div class="form-group mb-3">
            <%= f.label :baladia, "Commune", class: "form-label" %>
            <%= f.select :baladia, @selected_wilaya, {}, class: "form-control", required: true %>
          </div>
          <div class="form-group mb-3">
            <%= f.label :password, "Mot de passe", class: "form-label" %>
            <%= f.password_field :password, class: "form-control", placeholder: "Tapez votre mot de passe", required: true %>
          </div>
          <div class="form-group mb-3">
            <%= f.label :password_confirmation, "Confirmer le mot de passe", class: "form-label" %>
            <%= f.password_field :password_confirmation, class: "form-control", placeholder: "Confirmer le mot de passe", required: true %>
          </div>
          <div class="d-grid">
            <%= f.submit "Soumettre", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
